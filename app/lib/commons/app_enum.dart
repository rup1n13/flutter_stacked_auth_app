// ignore_for_file: constant_identifier_names

/*
*  Application enum
* */

/// [data enum] use to define Apis and app global logic environment
enum Environment { dev, prod }

enum CaptureMode { card, selfie, kooli }


/// printer
enum ToastLength {
  short,
  medium,
  long,
}
enum ToastState {
  failed,
  warning,
  success,
}

enum KycState {
  VALID,
  PENDING,
  NONE,
  REJECTED,
}

